@startuml Flux_Double_Single_Stream
!theme plain
skinparam backgroundColor #FEFEFE
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 11
skinparam ArrowThickness 2
skinparam RoundCorner 8
skinparam PackageBorderThickness 2
skinparam NoteBackgroundColor #FFFFDD
skinparam NoteBorderColor #999999

title **Flux Transformer 核心: Double Stream → Single Stream**\n以 Flux 1 代码为主线展开, Flux 2 差异全标注\nFlux 1: flux/transformer.py | Flux 2: flux2/transformer.py

' ============================================================
' 前置: 输入预处理
' ============================================================
package "**前置: 输入预处理**\n//transformer.py:677-711//" as PKG_PRE #E8EAF6 {

    rectangle "**x_embedder: 线性投影**\n//L677//\n\nhidden_states = self.x_embedder(hidden_states)\n\n**Flux 1:** [B, 4096, 64] → [B, 4096, **3072**]\n  3072 = 24 heads × 128 head_dim\n\n**Flux 2:** [B, 1024, 512] → [B, 1024, **6144**]\n  6144 = 48 heads × 128 head_dim" as X_EMBED #C5CAE9

    rectangle "**context_embedder: 文本投影**\n//L701//\n\n**Flux 1:** T5嵌入 [B, 512, 4096] → [B, 512, **3072**]\n**Flux 2:** Mistral多层嵌入 [B, 512, 15360] → [B, 512, **6144**]\n\n投影到和图像 token **同一维度**\n这样才能做 Joint Attention\n\n**Flux 2 的 15360 = 5120 × 3层**\nMistral 第10/20/30层特征拼接" as CTX_EMBED #9FA8DA

    rectangle "**temb: 条件嵌入向量 ★ 详解**\n\n**Flux 1:** //transformer.py:694-698//\n  CombinedTimestepGuidanceTextProjEmbeddings\n  temb = time_text_embed(\n      timestep × 1000,\n      guidance × 1000,\n      pooled_projections  // CLIP-L pooled [768]\n  )\n  **内部计算:**\n    t_emb = Sinusoidal(timestep) → MLP → [3072]\n    g_emb = Sinusoidal(guidance) → MLP → [3072]\n    p_emb = SiLU(Linear(pooled)) → Linear → [3072]\n    temb = t_emb + g_emb + p_emb\n  **三个信号相加成一个 [B, 3072] 向量**\n\n**Flux 2:** //flux2/transformer.py:960//\n  Flux2TimestepGuidanceEmbeddings\n  temb = time_guidance_embed(timestep, guidance)\n  **只有时间步+guidance, 没有pooled文本!**\n    t_emb = Sinusoidal(timestep) → MLP → [6144]\n    g_emb = Sinusoidal(guidance) → MLP → [6144]\n    temb = t_emb + g_emb\n  **Flux 2 不把CLIP pooled混入temb**\n  (因为根本没有CLIP, 只有Mistral)" as TEMB #7986CB

note right of TEMB
  **temb 到底是什么? 为什么重要?**

  temb = **T**imestep **EMB**edding 的缩写
  它是一个浓缩了"当前去噪状态"的向量

  **它回答的问题:**
  "现在噪声有多大?" (timestep)
  "生成要多严格?" (guidance)
  "整体要画什么?" (pooled text, Flux1 only)

  **它怎么影响每一层:**
  temb → Linear → 6个参数:
    shift_msa, scale_msa, **gate_msa**
    shift_mlp, scale_mlp, **gate_mlp**

  shift/scale: 调整LayerNorm的输出分布
    <latex>\hat{x} = (1 + \gamma) \cdot \mathrm{Norm}(x) + \beta</latex>
    "这一层应该关注什么范围的特征"

  **gate: 控制这一层输出的强度** <latex>[0, +\infty)</latex>
    <latex>\mathrm{gate} \approx 0</latex> → 跳过这一层 (输出被消除)
    <latex>\mathrm{gate} \approx 1</latex> → 正常贡献
    <latex>\mathrm{gate} > 1</latex> → 放大贡献

  **gate让模型自己学会:**
  - 高噪声步: 某些层的gate大 (需要大幅去噪)
  - 低噪声步: 那些层的gate小 (精修阶段不需要)
  - 不同guidance: 调整哪些层更活跃

  ════════════════════════════════════════
  **★ gate 公式逐层拆解: 沿数据流方向剥洋葱**
  ════════════════════════════════════════

  完整公式: gate = chunk( **Linear**( **SiLU**(temb) ) )[slice]

  **第①层: temb — 基底信号**
  输入是"当前去噪状态"的浓缩向量 [B, dim]
  它告诉模型: 现在是纯噪声的第1000步,
  还是快出清晰图像的第10步

  **第②层: SiLU(temb) — 非线性预处理**
  <latex>f(x) = x \cdot \sigma(x) = \frac{x}{1 + e^{-x}}</latex>
  **SiLU 不是gate的激活函数!**
  它是对 temb 的"非线性滤镜":
    让模型更平滑地区分不同时间步的特征
    负值被柔和地压制, 正值被轻微放大
  处理完后把结果交给下一层, 使命就结束了

  **第③层: Linear — 无界的线性投影**
  <latex>y = Wx + b</latex>
  W 和 b 是自由学习的实数
  → 输出值域 <latex>(-\infty, +\infty)</latex>
  **关键: Linear之后没有任何激活函数限制输出!**
  不像 LSTM 用 Sigmoid 把门控压到 [0,1]
  这里直接原封不动地输出

  **第④层: chunk — 高效切片**
  为了效率, 不跑6次独立的Linear
  而是一次性输出6倍长的向量, 再均等切开:
  emb.chunk(6) → shift_msa | scale_msa | **gate_msa**
                  shift_mlp | scale_mlp | **gate_mlp**
  gate 就是其中的一个切片, 继承了Linear的无界性

  **合在一起看:**
  temb → [SiLU非线性预处理] → [Linear无界投影] → [切片取gate]
  全程没有 Sigmoid/Tanh 限制 → **gate 是自由的缩放因子**

  **★ 这和传统门控的本质区别:**
  传统 (LSTM): gate = **Sigmoid**(Wx+b) ∈ **[0, 1]** → 只能开/关
  Flux adaLN: gate = **Linear**(SiLU(temb)) ∈ **(-∞, +∞)** → 能开/关/放大
  少了一个 Sigmoid, 从"开关"升级为"连续调节旋钮"
end note

    rectangle "**RoPE: 旋转位置编码 ★ 详解**\n//L709-711//\n\nids = cat(txt_ids, img_ids)  // [4608, 3]\nimage_rotary_emb = pos_embed(ids)\n\n**Step 1: 构建位置ID**\n//transformer.py:1010-1026//\n\n  txt_ids = zeros(512, 3)  // 文本: 全零, 没有空间位置\n  img_ids = zeros(H/2, W/2, 3):\n    img_ids[..., 0] = 0           // 第0维: 预留(Flux1恒为0)\n    img_ids[..., 1] = arange(H/2) // 第1维: **y坐标** (行号)\n    img_ids[..., 2] = arange(W/2) // 第2维: **x坐标** (列号)\n  → reshape为 [4096, 3]\n\n**Step 2: 对每个维度独立生成旋转频率**\n//FluxPosEmbed.forward() — diffusers/embeddings.py:1245//\n\n  for i in range(3):  // 3个轴\n    cos_i, sin_i = get_1d_rotary_pos_embed(\n      dim=axes_dim[i],  // Flux1: (16, 56, 56)\n      pos=ids[:, i],     // 该轴的坐标值\n      theta=10000\n    )\n  freqs_cos = cat([cos_0, cos_1, cos_2])  // [4608, 128]\n  freqs_sin = cat([sin_0, sin_1, sin_2])  // [4608, 128]\n\n**Step 3: 在Attention中旋转Q和K**\n//FluxAttnProcessor2_0, L150-151//\n\n  Q = apply_rotary_emb(Q, (freqs_cos, freqs_sin))\n  K = apply_rotary_emb(K, (freqs_cos, freqs_sin))" as ROPE #5C6BC0

note right of ROPE
  **RoPE 核心原理: 用旋转编码相对位置**

  **1D RoPE (LLM文本):**
  给位置 m 的token, 将Q/K向量的
  每对相邻元素 <latex>(q_{2i},\; q_{2i+1})</latex> 视为复数,
  乘以旋转因子 <latex>e^{i \cdot m \cdot \theta_k}</latex>:

  <latex>q' = q \cos(m\theta) + \mathrm{rotate}(q) \sin(m\theta)</latex>

  <latex>\theta_k = \frac{1}{10000^{2k/d}}</latex>
  **低频维度变化慢(捕捉远距离), 高频维度变化快(捕捉近距离)**

  关键性质: <latex>\langle Q_m, K_n \rangle</latex> 只依赖于 <latex>m - n</latex> (相对距离)
  → 位置信息自动编码进注意力分数中

  **2D RoPE (Flux图像):**
  图像token有 (y, x) 两个坐标
  **把head_dim切成几段, 每段编码一个轴:**

  Flux 1: head_dim=128, axes_dim=(16, 56, 56)
    Q[..., 0:16]   → 用 pos[:,0]=0 旋转 (预留维度)
    Q[..., 16:72]  → 用 pos[:,1]=**y坐标** 旋转
    Q[..., 72:128] → 用 pos[:,2]=**x坐标** 旋转

  这样 <latex>\langle Q(y_1,x_1),\; K(y_2,x_2) \rangle</latex> 的注意力分数
  自动编码了 <latex>\Delta y = y_1 - y_2</latex> 和 <latex>\Delta x = x_1 - x_2</latex>
  → 模型天然感知二维空间的相对位置

  **Flux 2: axes_dim=(32, 32, 32, 32) — 4维 (T, H, W, L)!**
  //pipeline.py:943-991//
  4维是 **(时间T, 高度H, 宽度W, 序列索引L)**:

  **纯T2I — 图像token的ids:**
    _prepare_latent_ids(): cartesian_prod(t=0, h=arange(H), w=arange(W), lat=0)
    → (T=0, H=y坐标, W=x坐标, **L=0**)
    图像token: 空间位置靠H,W编码, T和L恒为0

  **纯T2I — 文本token的ids: ★ Flux 2 重大变化!**
    _prepare_text_ids(): cartesian_prod(t=0, h=0, w=0, lat=arange(L))
    → (T=0, H=0, W=0, **L=序列位置 0,1,2,...,511**)
    **文本不再全零!** 第4维编码了文本token的顺序

  **Flux 1 vs Flux 2 文本位置编码对比:**
  Flux 1: txt_ids = zeros → RoPE对文本完全不起作用
    文本顺序完全靠T5自带的位置编码
  Flux 2: txt_ids 在L维有序号 → RoPE也编码文本顺序
    Attention层额外获得一层文本顺序信息
    → **Joint Attention中图文对齐更精确**

  **★ T维的真正用途: 区分多张参考图 (不是视频帧!)**
  //pipeline.py:994-1041, __init__.py:195-203//

  纯T2I: T=0, 不起作用
  **图像编辑/多参考图场景:** _prepare_image_ids()
    第1张参考图: T = scale + scale×0 = **10**
    第2张参考图: T = scale + scale×1 = **20**
    第3张参考图: T = scale + scale×2 = **30**
    代码注释: "T-coordinate: scale + scale * ref_idx
               (to distinguish reference images)"

  → 同一张参考图内: ΔT=0, RoPE在T维不区分
  → 不同参考图之间: ΔT≠0, RoPE自动编码"来自不同图"
  → **用位置编码而非额外标记来区分多图身份**

  Q[..., 0:32]   → T维旋转 (**多图身份区分**, 纯T2I时=0)
  Q[..., 32:64]  → H维旋转 (**y坐标**)
  Q[..., 64:96]  → W维旋转 (**x坐标**)
  Q[..., 96:128] → L维旋转 (**序列索引**)

  **⚠️ theta差异:**
  Flux 1: theta=**10000** (和LLM标准一致)
  Flux 2: theta=**2000** (更小 → 频率更高 → 衰减更快)
  → Flux 2对**近距离位置差异更敏感**
    远距离token的位置区分度降低
    配合更宽的通道(6144)补偿表达力

  **⚠️ RoPE计算流程差异:**
  Flux 1: ids = cat(txt_ids, img_ids) → **一起**过pos_embed
  Flux 2: image_rope = pos_embed(**img_ids**)
          text_rope  = pos_embed(**txt_ids**)
          concat = cat(text_rope, image_rope)
          → **分别**计算后拼接

  **3D RoPE (视频模型, 如HunyuanVideo):**
  视频token有 (t, y, x) 三个坐标
  axes_dim=(16, 56, 56):
    Q[..., 0:16]   → 用 **帧号t** 旋转
    Q[..., 16:72]  → 用 **y坐标** 旋转
    Q[..., 72:128] → 用 **x坐标** 旋转

  <latex>\langle Q(t_1,y_1,x_1),\; K(t_2,y_2,x_2) \rangle</latex>
  自动编码 <latex>\Delta t,\; \Delta y,\; \Delta x</latex> 三个方向的相对位置
  → 时空注意力天然感知"多远的帧+多远的位置"

  **Flux 1 文本ids为什么全零?**
  txt_ids = zeros(512, 3)
  → 所有文本token的位置 = (0, 0, 0)
  → 文本之间: <latex>\Delta=0</latex>, RoPE不影响 (<latex>\cos(0)=1,\;\sin(0)=0</latex>)
  → 文本和图像之间: 只有图像侧的坐标起作用
  → **文本没有空间位置, 这是正确的!**
  → 文本的顺序关系靠T5自带的位置编码

  **vs 绝对位置编码 (SDXL的Sinusoidal):**
  绝对: pos_embed[m] 加到token上, 位置是全局的
    → 训练1024, 推理2048 = 没见过的位置 = 崩溃
  旋转: 编码的是**相对距离**, 不是绝对位置
    → 训练1024, 推理2048 = 相对距离仍有意义
    → **天然支持可变分辨率!**
    这就是Flux能灵活切换分辨率的数学基础

  **完整数学推导 (含旋转矩阵、内积证明):**
  **→ 见 07_rope_mathematical_derivation.puml**
end note
}

' ============================================================
' 第一阶段: Double Stream Blocks (19层)
' ============================================================
package "**第一阶段: Double Stream Blocks × 19**\n//transformer.py:753-850//\nFluxTransformerBlock" as PKG_DOUBLE #FCE4EC {

    rectangle "**两条独立的流, 各自有独立的 Norm 和 FFN**\n\n**Flux 1:** //transformer.py:302-304, 324-328//\n**图像流:**\n  norm1 → **AdaLayerNormZero**(dim)  // 内置SiLU+Linear调制\n  norm2 → LayerNorm  +  ff → **FeedForward** (GELU, 4x升维)\n**文本流:**\n  norm1_context → **AdaLayerNormZero**(dim)\n  norm2_context → LayerNorm  +  ff_context → **FeedForward** (GELU)\n**Attention:**\n  self.attn → Attention + **FluxAttnProcessor2_0**\n  内部两套投影: to_q/k/v + add_q/k/v_proj\n\n**Flux 2:** //flux2/transformer.py:528-561//\n**图像流:**\n  norm1 → **nn.LayerNorm**(dim)  // 普通LN, 调制从外部传入\n  norm2 → LayerNorm  +  ff → **Flux2FeedForward** (SwiGLU, 3x升维)\n**文本流:**\n  norm1_context → **nn.LayerNorm**(dim)\n  norm2_context → LayerNorm  +  ff_context → **Flux2FeedForward** (SwiGLU)\n**Attention:**\n  self.attn → **Flux2Attention** + **Flux2AttnProcessor**\n  同样两套投影, 但QK Norm用独立模块(非attn内置rms_norm)\n\n**两版本的拓扑结构完全相同:**\n两条流 × 各自norm+FFN × 共享一个Attention\n**但组件类型全换了: Norm方式/FFN激活/Attention后端**" as DOUBLE_INIT #F8BBD0

    rectangle "**Step 1: 条件调制 ★ Flux1 vs Flux2 关键差异**\n\n**Flux 1:** //transformer.py:342-346//\n  **AdaLayerNormZero** — 每个block**内部自带**temb投影\n  //diffusers/models/normalization.py//\n  **门控函数内部实现:**\n    emb = **Linear**(dim→6×dim)( **SiLU**(temb) )\n    shift_msa, scale_msa, **gate_msa**,\n    shift_mlp, scale_mlp, **gate_mlp** = emb.chunk(**6**, dim=1)\n    x = LayerNorm(x) × (1+scale_msa) + shift_msa\n    return x, gate_msa, shift_mlp, scale_mlp, gate_mlp\n  **每个block有独立的 Linear(dim→6×dim) 权重!**\n  19层×2流 = **38组独立的 SiLU→Linear 投影**\n  → 每层可以**独立学习**怎么响应temb\n  gate 形状: [B, dim], 使用时需 .unsqueeze(1)\n\n**Flux 2:** //flux2/transformer.py:680-696, 791-794//\n  **Flux2Modulation** — 全局**共享**调制模块\n  **门控函数内部实现:**\n    mod = **Linear**(dim→dim×3×n)( **SiLU**(temb) )\n    if mod.ndim == 2: mod = mod.unsqueeze(1)  // → [B,**1**,dim×3×n]\n    chunks = mod.chunk(3×n, dim=-1)\n    return ((shift_msa,scale_msa,**gate_msa**),(shift_mlp,scale_mlp,**gate_mlp**))\n  **整个模型只有3个Modulation实例:**\n    double_stream_modulation_**img** (mod_param_sets=2)\n    double_stream_modulation_**txt** (mod_param_sets=2)\n    single_stream_modulation (mod_param_sets=1)\n  **所有double block收到完全相同的gate值!**\n  gate 形状: [B, **1**, dim], 无需unsqueeze直接乘\n\n**★ 两者的gate激活函数本质相同:**\n  gate = Linear( SiLU(temb) ) 的分片\n  **没有sigmoid/tanh! gate是无界的原始线性输出**\n  SiLU作用于temb(输入), 不是gate(输出)\n  gate ∈ (-∞, +∞), 但训练中趋向 [0, ~2]" as DOUBLE_NORM #F48FB1

    rectangle "**Step 2: Joint Attention ★ 灵魂所在**\n//transformer.py:355-360//\n→ FluxAttnProcessor2_0\n//transformer.py:95-190//\n\nattn_output, context_attn_output = self.attn(\n    hidden_states = norm_img,\n    encoder_hidden_states = norm_txt,\n    image_rotary_emb = rope,\n)\n\n**展开见右侧详解 →**" as DOUBLE_ATTN #EC407A

    rectangle "**Step 3: 图像流后处理 — 门控 + FFN + 残差流**\n\n**Flux 1:** //transformer.py:367-378//\n**── 第一次残差: Attention ──**\nattn_output = **gate_msa.unsqueeze(1)** × attn_output\n  // gate: [B,dim]→[B,**1**,dim] × attn: [B,4096,dim]\nhidden_states = hidden_states **+** attn_output\n\n**── FFN (FeedForward) ──**\nnorm_img = LayerNorm(hidden_states)\nnorm_img = norm_img × (1 + **scale_mlp**) + **shift_mlp**\nff_output = **FeedForward**(norm_img):\n  Linear(3072 → **12288**) + GELU + Linear(**12288** → 3072)\n\n**── 第二次残差: FFN ──**\nff_output = **gate_mlp.unsqueeze(1)** × ff_output\nhidden_states = hidden_states **+** ff_output\n\n**Flux 2:** //flux2/transformer.py:597-604//\n**同样的结构, 但两处不同:**\n  1. gate已是[B,**1**,dim], **无需unsqueeze**\n     attn_output = **gate_msa** × attn_output  // 直接乘\n  2. FFN用 **Flux2FeedForward** (SwiGLU):\n     Linear(6144→**36864**) → SwiGLU → Linear(**18432**→6144)\n     SwiGLU内部: SiLU(x1) ⊙ x2 (x1,x2各18432维)" as DOUBLE_IMG_POST #E91E63

note right of DOUBLE_IMG_POST
  **残差流: 贯穿57层的主线**

  hidden_states ═══════════════════════
       │                              ↑
       ↓                              │
    [Attention]                       │
       │                              │
       ↓                              │
    × gate_msa ──────(+)──────────────┘ 第一次残差
       │
  hidden_states ═══════════════════════
       │                              ↑
       ↓                              │
    [FFN: 3072→12288→3072]            │
       │                              │
       ↓                              │
    × gate_mlp ──────(+)──────────────┘ 第二次残差
       │
  hidden_states ═══════ → 下一层

  **"═══" 就是残差流**
  Attention和FFN只提供修正量δ
  gate控制δ的大小
  δ加到主线上, 主线永远不断

  **FFN 的升维→降维:**
  3072维: 信息"太窄", 非线性不充分
  升到12288: 给GELU足够的空间变换
  降回3072: 和残差流对齐

  **Flux 2 FFN 差异 (Flux2FeedForward + Flux2SwiGLU):**
  //flux2/transformer.py:123-162//
  mlp_ratio=**3.0** (不是4.0)
  **Flux2FeedForward:**
    Linear(6144 → **18432×2=36864**)  // 升维×2 因为SwiGLU要split
    → **Flux2SwiGLU:**
        x1, x2 = x.chunk(2)  // 36864 → 两个18432
        output = **SiLU(x1) × x2**  // 门控乘法 → 18432
    → Linear(**18432** → 6144)  // 降回原维

  **SwiGLU 的门控机制:**
  不同于GELU的 x×Φ(x) (自门控)
  SwiGLU 用**两路独立的线性投影**:
    x1 = W1·x  (门控路径, 过SiLU)
    x2 = W2·x  (值路径, 不过激活)
    output = SiLU(x1) ⊙ x2
  → x1决定"开关强度", x2携带"信息内容"
  → 比GELU多一倍FFN输入参数, 但效果更好

  **⚠️ 注意: 这里的SiLU门控和temb门控是两回事!**
  temb gate: 控制**整层输出**的残差贡献强度
  SwiGLU SiLU: 控制**FFN内部**的信息流通路径
  两者独立工作, 在不同层级调控信息流
end note

    rectangle "**Step 4: 文本流后处理 (对称结构)**\n//transformer.py:381-388//\n\ncontext_attn = **c_gate_msa** × context_attn_output\nencoder_hidden_states += context_attn  // 残差\n\nnorm_txt = norm2_context(encoder_hidden_states)\nnorm_txt = norm_txt × (1+c_scale) + c_shift\ncontext_ff = ff_context(norm_txt)\nencoder_hidden_states += **c_gate_mlp** × context_ff\n\n**文本表示被图像信息更新了!**\n这是和 SDXL Cross-Attention 的本质区别" as DOUBLE_TXT_POST #C62828

    rectangle "**return encoder_hidden_states, hidden_states**\n//transformer.py:394//\n\n两条流各自更新, 各自返回\n进入下一个 Double Block 继续处理" as DOUBLE_RETURN #B71C1C
}

note left of DOUBLE_NORM
  **架构迭代: 调制方式的权衡**

  **Flux 1 的选择: 每层独立调制**
    每个 AdaLayerNormZero 内置:
      self.silu = nn.SiLU()
      self.linear = nn.Linear(dim, **6**×dim)
    19个double block × 2流 = **38个独立Linear**
    + 38个single block 各有 Linear(dim, **3**×dim)
    **调制参数总量:**
      Double: 38 × (3072 × 6×3072) ≈ **2.16B 参数**
      Single: 38 × (3072 × 3×3072) ≈ **1.08B 参数**
    **优点:** 每层可以学到不同的调制策略
      浅层gate≈0: 跳过(高噪声不需要精修)
      深层gate≈1: 正常贡献(精修细节)
    **代价:** 参数量庞大

  **Flux 2 的选择: 全局共享调制**
    **只有3个Flux2Modulation实例** (L791-796):
      double_stream_modulation_img: Linear(6144, 6144×6)
      double_stream_modulation_txt: Linear(6144, 6144×6)
      single_stream_modulation:     Linear(6144, 6144×3)
    **调制参数总量:**
      3个Linear ≈ **0.68B 参数** (vs Flux1的3.24B)
    **代价:** 所有层收到**完全相同的gate值**
      第0层和第7层的gate_msa一模一样!
      靠更宽的隐藏层(6144)来补偿表达力

  **设计哲学的转变:**
  Flux 1: "每层精细控制" → 深度优先
  Flux 2: "统一控制+更宽通道" → 宽度优先
  这反映了 Scaling Law 的工程实践:
  **加宽比加深更容易并行, 训练更稳定**

  ────────────────────────────────
  **★ "AdaLayerNormZero" 名字的来源与初始化真相**

  "Zero" 来自 DiT 论文 (Peebles & Xie, 2022)
  原始设计意图: **初始化时** Linear 权重全部置零
  → 初始 gate = 0 → 每个block启动时是恒等函数
  → 训练开始时, 整个Transformer = 直通管道
  → 模型从"什么都不做"渐进地学会每层的职责

  **源码实证: 不同模型的初始化差异**
  ✅ HunYuanVideo: nn.init.**zeros_**(self.linear.weight)
  ✅ HiDream:      nn.init.**zeros_**(self.adaLN_modulation[1].weight)
  ❌ **Flux 2 的 Flux2Modulation: 没有零初始化!**
     用的是 PyTorch 默认的 Kaiming Uniform
  → "Zero"是设计哲学的名字, **不是所有实现都遵守**
  ────────────────────────────────

  **★ 对 LoRA 训练的影响:**
  Flux 1: 每层的gate独立 → LoRA可以
    针对不同层学到不同的调制偏移
  Flux 2: 所有层gate相同 → LoRA对调制的
    影响是"一刀切"的, 但block内部的
    attention/FFN权重仍然是每层独立的
end note

note right of DOUBLE_ATTN
  **FluxAttnProcessor2_0 内部 ★**
  //transformer.py:106-190//

  **1. 图像流 QKV 生成:**
  Q_img = to_q(norm_img)    // [B, heads, 4096, 128]
  K_img = to_k(norm_img)
  V_img = to_v(norm_img)

  **2. 文本流 QKV 生成 (独立投影!):**
  Q_txt = **add_q_proj**(norm_txt)  // [B, heads, 512, 128]
  K_txt = **add_k_proj**(norm_txt)
  V_txt = **add_v_proj**(norm_txt)

  **注意:** 图像用 to_q/k/v
  文本用 **add_q/k/v_proj** ← 独立的权重矩阵!

  **3. 拼接 ★ ★ ★**
  //L145-147//
  Q = cat(**[Q_txt, Q_img]**, dim=seq)  // [B, h, **4608**, 128]
  K = cat(**[K_txt, K_img]**, dim=seq)
  V = cat(**[V_txt, V_img]**, dim=seq)

  **文本在前, 图像在后!**

  **4. 应用 RoPE:**
  //L150-151//
  Q = apply_rotary_emb(Q, rope)
  K = apply_rotary_emb(K, rope)

  **5. Scaled Dot-Product Attention:**
  //L166-173//
  output = F.scaled_dot_product_attention(Q, K, V)
  // → [B, heads, **4608**, 128]

  **4608 = 512(文本) + 4096(图像)**
  每个token都能看到所有其他token!
  图像看图像 + 图像看文本
  文本看文本 + **文本看图像** ← 关键!

  **6. 拆分回两条流:**
  //L178-179//
  txt_out = output[:, **:512**, :]   // 文本部分
  img_out = output[:, **512:**, :]   // 图像部分

  **7. 各自输出投影:**
  img_out = to_out(img_out)      // 图像用 to_out
  txt_out = **to_add_out**(txt_out)  // 文本用 add_out

  return img_out, txt_out
end note

note left of DOUBLE_ATTN
  **Flux 2 Joint Attention 的7处关键差异 ★**
  //flux2/transformer.py:165-239, 242-314//

  **差异①: QK Norm — 条件执行 vs 无条件执行**
  Flux 1: **if** attn.norm_q **is not None**: norm_q(query)
    (norm可能不存在, 需要守卫判断)
  Flux 2: attn.norm_q(query) **直接调用, 无if**
    (构造函数**总是**创建norm_q/norm_k)

  **差异②: QK Norm 的定义与可学习性**
  Flux 1: qk_norm="rms_norm" 传入 diffusers Attention
    norm 在 Attention **内部**创建
  Flux 2: **自己定义**独立的 RMSNorm:
    norm_q = torch.nn.**RMSNorm**(dim_head,
      **elementwise_affine=True**)
    **有可学习的缩放参数 γ!**
    图像流/文本流各有独立的 norm 参数

  **差异③: 张量布局完全不同 ★★ 最关键!**
  Flux 1: query.view(B,-1,h,d).**transpose**(1,2)
    → [B, **heads**, seq, dim]  // heads在前
  Flux 2: query.**unflatten**(-1, (h,-1))
    → [B, **seq**, heads, dim]  // seq在前!
  **不做transpose!** 后续RoPE、Attention、
  输出reshape全部适配不同布局
  seq维度在前 → 利于FlashAttention分块

  **差异④: RoPE 应用方式**
  Flux 1: **_apply_rotary_emb_anyshape**(q, rope)
    SimpleTuner自定义, 支持批量化RoPE
  Flux 2: **apply_rotary_emb**(q, rope, **sequence_dim=1**)
    diffusers标准函数 + 显式序列维度

  **差异⑤: Attention 后端**
  Flux 1: 硬编码 F.**scaled_dot_product_attention**()
  Flux 2: **dispatch_attention_fn**(Q, K, V,
    backend=self._attention_backend)
    运行时切换 SDPA / FlashAttn / 其他

  **差异⑥: 输出拆分**
  Flux 1: 切片 hidden_states[:, :txt_len]
  Flux 2: .**split_with_sizes**([txt_len, img_len])

  **差异⑦: 输出投影顺序**
  Flux 1: **先img** to_out → **后txt** to_add_out
  Flux 2: **先txt** to_add_out → **后img** to_out
  顺序反了! 数值不影响(独立投影)

  **额外: QKV 融合投影 (Flux 2 独有)**
  Q,K,V = attn.**to_qkv**(x).chunk(3)
  一次Linear出QKV, 比3次Linear更高效

  **总结: 宏观流程一致, 微观实现全不同**
  张量布局、Norm定义、RoPE函数、后端调度
  **Flux 2 方向: 更显式、更灵活、更GPU友好**
end note

note left of DOUBLE_TXT_POST
  **这里是 Flux 和 SDXL 的本质区别:**

  **SDXL Cross-Attention:**
  文本嵌入是**冻结的输入**
  经过所有 UNet 层后
  文本嵌入**没有变化**
  信息单向流动: 文本→图像

  **Flux Double Stream:**
  文本嵌入会被**图像信息更新**
  每经过一层, 文本表示都在变化
  信息**双向流动**: 文本↔图像

  **意义:**
  "a red car next to a blue house"
  第1层: "red"token还不知道car长什么样
  第5层: "red"token的表示已经融入了
         这张具体图片里car的视觉特征
  第19层: "red car"的文本表示已经完全
          和图片中实际的红色汽车对齐了

  **这就是Flux文本-图像对齐更精准的原因**
end note

' ============================================================
' 过渡: 拼接
' ============================================================
rectangle "**══ 过渡: 两流合并 ══**\n//transformer.py:854//\n\nhidden_states = **cat([txt, img]**, dim=1)\n\n**Flux 1:** [B, 512, 3072] + [B, 4096, 3072] → [B, **4608**, 3072]\n**Flux 2:** [B, 512, 6144] + [B, 1024, 6144] → [B, **1536**, 6144]\n\n**从此刻起, 图像和文本不再区分**\n变成一个统一的序列" as MERGE #FF6F00

note right of MERGE
  **为什么在这里合并?**

  Double Stream 的 19 层已经完成了:
  - 图像建立了自身的空间表征
  - 文本建立了自身的语义表征
  - 两者通过 Joint Attention 充分交换了信息

  此时文本和图像的表示已经
  在**同一个语义空间**里了
  (都是 3072 维, 且经过 19 层对齐)

  **合并后可以做更深度的融合:**
  Single Stream 不再区分谁是图像谁是文本
  统一当做 4608 个 token 处理
end note

' ============================================================
' 第二阶段: Single Stream Blocks (38层)
' ============================================================
package "**第二阶段: Single Stream Blocks × 38**\n//transformer.py:857-977//\nFluxSingleTransformerBlock" as PKG_SINGLE #E8F5E9 {

    rectangle "**统一序列输入**\n\nhidden_states: [B, **4608**, 3072]\n前512个token = 文本\n后4096个token = 图像\n\n**但Block不知道也不关心这个区分**\n统一处理所有 token" as SINGLE_INPUT #C8E6C9

    rectangle "**Step 1: 条件调制 ★ Single Stream 门控详解**\n\n**Flux 1:** //transformer.py:257//\n  **AdaLayerNormZeroSingle** — 每个block**内部自带**投影\n  //diffusers/models/normalization.py//\n    emb = **Linear**(dim→**3**×dim)( **SiLU**(temb) )\n    shift_msa, scale_msa, **gate_msa** = emb.chunk(**3**, dim=1)\n    x = LayerNorm(x) × (1+scale_msa) + shift_msa\n    return x, gate_msa   // gate: [B, dim]\n  **只有1个gate** (因为Attn+MLP并行后合并投影)\n  gate用法: gate.unsqueeze(1) × proj_out(cat(attn,mlp))\n  **38个block各有独立的 Linear(3072→9216) 权重**\n\n**Flux 2:** //flux2/transformer.py:505-517//\n  **共享 single_stream_modulation** (mod_param_sets=1)\n    (shift, scale, **gate**) = single_stream_modulation(temb)[0]\n    x = (1+scale) × LayerNorm(x) + shift\n  gate用法: hidden_states + **gate** × attn_output\n  **48个single block收到完全相同的gate值!**\n  gate: [B, 1, dim], 无需unsqueeze\n\n**★ 关键差异: gate的作用位置不同!**\n  Flux 1: gate × proj_out(**cat(attn, mlp)**) → 门控整个输出\n  Flux 2: gate × attn_output → 门控的是**已经融合了MLP的**输出\n  (因为Flux2的ParallelSelfAttention内部已融合attn+mlp)" as SINGLE_NORM #A5D6A7

    rectangle "**Step 2: 并行 Attention + MLP ★ Flux1 vs Flux2 差异**\n\n**Flux 1:** //transformer.py:258-272//\n  MLP 分支: mlp_out = GELU(proj_mlp(norm))\n  Attn 分支: attn_out = attn(norm, encoder=**None**)\n  output = **cat([attn_out, mlp_out]**, dim=-1)\n  → proj_out 压缩回工作维度\n  **MLP和Attention在外部并行, 结果拼接**\n\n**Flux 2:** //flux2/transformer.py:476-486//\n  Flux2**ParallelSelf**Attention 内部:\n  QKV投影和MLP投影**融合在同一个线性层**!\n  一次矩阵乘法同时出 Q,K,V + MLP_hidden\n  **并行不是在外部cat, 而是在投影层就融合了**\n  → 更少的内存访问, 更高的GPU利用率\n\n两者都是 encoder_hidden_states=None\n→ 纯 Self-Attention, 4608/1536 tokens互看" as SINGLE_ATTN #81C784

    rectangle "**Step 3: 投影 + Gate + 残差 ★ Flux1 vs Flux2 差异**\n\n**Flux 1:** //transformer.py:273-275//\n  output = cat([attn_out, mlp_out], dim=2)\n  gate = gate_msa.**unsqueeze(1)**  // [B,dim]→[B,1,dim]\n  hidden_states = **gate** × proj_out(output)\n  hidden_states = **residual + hidden_states**\n  proj_out: [3072+12288=**15360**] → [3072]\n  **gate门控的是 Attn+MLP 合并投影后的整个输出**\n\n**Flux 2:** //flux2/transformer.py:517//\n  attn_output = ParallelSelfAttn(norm_x)  // 内部已融合attn+mlp\n  hidden_states = hidden_states + **mod_gate** × attn_output\n  **gate门控的是已融合attn+mlp的输出**\n  mod_gate: [B,1,dim], 直接乘, 无需unsqueeze\n\n**公式对比:**\n  Flux 1: x = x + gate × W_out(concat(Attn(x̂), MLP(x̂)))\n  Flux 2: x = x + gate × ParallelAttnMLP(x̂)" as SINGLE_POST #66BB6A
}

note right of SINGLE_ATTN
  **Single Stream 的关键设计:**

  **1. Attention + MLP 是并行的, 不是串行!**

  Double Stream: Attention → 残差 → MLP → 残差
  Single Stream: Attention 和 MLP **同时算**
                 结果 cat 后一起投影

  **为什么?** 并行计算更快
  GPU 可以同时执行两个分支
  最后 cat + proj_out = 隐式的融合

  **2. 没有 Cross-Attention!**

  encoder_hidden_states = None
  → 只做 Self-Attention
  → 4608 个 token 互相看

  但前 512 个 token 是文本!
  所以 Self-Attention 自然包含了:
  - 图像看图像 (空间关系)
  - 文本看文本 (语义连贯)
  - **图像看文本** (内容生成)
  - **文本看图像** (上下文对齐)

  **合并后不需要Cross-Attention**
  Self-Attention 自动覆盖了所有交互
end note

note left of SINGLE_INPUT
  **38层 vs 19层:**

  Single Stream 的层数是 Double 的 2倍
  因为每层更轻量 (没有两套独立的FFN)
  但信息融合更深入

  **直觉:**
  Double Stream 19层 = "谈判阶段"
    图文各自有立场, 通过会议交换意见
    但会后各自回自己的办公室

  Single Stream 38层 = "协作阶段"
    图文坐在同一间办公室
    不分你我, 一起干活
end note

' ============================================================
' 后处理: 提取图像token
' ============================================================
package "**后处理: 提取图像 token**\n//transformer.py:979-982//" as PKG_POST #E8EAF6 {

    rectangle "**丢弃文本, 只保留图像**\n//L979//\n\nhidden_states = hidden_states[:, **txt_len:**, ...]\n// [B, 4608, 3072] → [B, **4096**, 3072]\n\n前512个文本token: **丢弃**\n后4096个图像token: **保留**\n\n**文本token完成了它的使命:**\n在57层里引导图像生成\n最终我们只需要图像预测\n\nnorm_out + proj_out:\n[B, 4096, 3072] → [B, 4096, **64**]\n压缩回 pack 维度, 准备 unpack" as EXTRACT #C5CAE9
}

' ============================================================
' 连接线
' ============================================================
X_EMBED -right-> CTX_EMBED
CTX_EMBED -right-> TEMB
TEMB -right-> ROPE

X_EMBED -down-> DOUBLE_INIT : "img: [B,4096,3072]"
CTX_EMBED -down-> DOUBLE_INIT : "txt: [B,512,3072]"

DOUBLE_INIT -down-> DOUBLE_NORM
DOUBLE_NORM -down-> DOUBLE_ATTN : "norm_img + norm_txt"
DOUBLE_ATTN -down-> DOUBLE_IMG_POST : "img_attn_out"
DOUBLE_ATTN -down-> DOUBLE_TXT_POST : "txt_attn_out"
DOUBLE_IMG_POST -down-> DOUBLE_RETURN
DOUBLE_TXT_POST -down-> DOUBLE_RETURN

DOUBLE_RETURN -down-> MERGE : "×19层循环后"

MERGE -down-> SINGLE_INPUT : "[B, 4608, 3072]"

SINGLE_INPUT -down-> SINGLE_NORM
SINGLE_NORM -down-> SINGLE_ATTN
SINGLE_ATTN -down-> SINGLE_POST

SINGLE_POST -down-> EXTRACT : "×38层循环后"

' ============================================================
' 底部总结
' ============================================================
note bottom of EXTRACT
  **Flux Transformer 完整数据流总结:**

  **输入预处理:**
  img [B,4096,64] → x_embedder → [B,4096,3072]
  txt [B,512,4096] → ctx_embedder → [B,512,3072]
  temb = timestep + guidance + CLIP-L pooled → [B,3072]
  rope = pos_embed(cat(txt_ids, img_ids))

  **Double Stream × 19层:**
  img 和 txt **分开存储, 共享 Attention**
  每层: AdaNorm → Joint Attention → 各自FFN → 各自残差
  Joint Attention: cat(Q_txt,Q_img) × cat(K_txt,K_img)^T × cat(V_txt,V_img)
  → 图文双向交互, 但各自保持独立的表示空间

  **合并: cat(txt, img) → [B, 4608, 3072]**

  **Single Stream × 38层:**
  统一序列, **不区分图文**
  每层: AdaNorm → Self-Attention(4608 tokens) **并行** MLP → cat → proj → 残差
  → 图文完全融合, 更深度的信息交换

  **提取: 丢弃文本token, 保留图像token**
  [B,4096,3072] → norm → proj → [B,4096,64] → unpack → [B,16,128,128]

  **Flux 1 vs Flux 2 实现差异总结:**

  | | Flux 1 | Flux 2 |
  |---|---|---|
  | Double Block类 | FluxTransformerBlock | **Flux2TransformerBlock** |
  | Single Block类 | FluxSingleTransformerBlock | **Flux2SingleTransformerBlock** |
  | Attention类 | FluxAttnProcessor2_0 | **Flux2AttnProcessor** |
  | 调制方式 | AdaLayerNormZero (每层独立) | **共享Modulation** (全层相同) |
  | **门控函数** | **SiLU→Linear→chunk (每层独立权重)** | **SiLU→Linear→chunk (全局共享权重)** |
  | **门控实例数** | **Double: 38个 + Single: 38个 = 76个** | **Double: 2个 + Single: 1个 = 仅3个** |
  | **gate值范围** | **(-∞,+∞) 无界, 训练趋向[0,~2]** | **(-∞,+∞) 无界, 同左** |
  | **gate形状** | **[B, dim], 需.unsqueeze(1)** | **[B, 1, dim], 内部已unsqueeze** |
  | **跨层gate差异** | **每层不同 (独立Linear权重)** | **所有层相同 (共享Linear权重)** |
  | FFN激活 | GELU(approximate="tanh") | **SwiGLU = SiLU(x1)×x2** |
  | QK Norm | rms_norm (attn内置) | **独立norm模块** |
  | Attention后端 | 硬编码 SDPA | **dispatch_attention_fn** (可切换) |
  | Single MLP融合 | 外部cat拼接 | **投影层内部融合** |
  | RoPE维度 | (16,56,56) 3维: 预留+Y+X | **(32,32,32,32) 4维: T+H+W+L(序列索引)** |
  | RoPE theta | 10000 (标准) | **2000** (更高频, 近距离敏感) |
  | RoPE文本ids | 全零(RoPE不影响文本) | **L维有序号(RoPE编码文本顺序)** |
  | RoPE计算 | cat(ids)后一起算 | **图文分别算再cat** |
  | inner_dim | 3072 | **6144** |

  **核心思想完全一致, 但工程实现全部重写**
  Flux 2 的每一处改动都指向同一个方向:
  **更宽的通道 + 更高效的计算 + 更少的冗余参数**

  **★ 门控函数总结 (两版本共同):**
  gate = chunk( **Linear**( **SiLU**(temb) ) )
  本质: temb先过SiLU非线性激活, 再线性投影, 最后切片取gate
  **SiLU不是gate的激活函数, 而是temb的预处理!**
  gate本身没有额外激活, 是原始的线性输出
  这意味着gate可以是负数(反转信号)或大于1(放大信号)
  但实际训练中, 模型学到的gate值大多在 [0, ~2] 范围内

  **LoRA 插入点 (两者相同):**
  Double Stream: to_q/k/v (图像) + add_q/k/v_proj (文本) + to_out + to_add_out
  Single Stream: to_q/k/v + to_out (图文统一)
end note

@enduml
